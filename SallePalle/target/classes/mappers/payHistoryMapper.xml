<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.mapper.PayHistoryMapper">

    <insert id="insertHistory">
        INSERT INTO pay_history(member_id, amount, type, memo)
        VALUES(#{member_id}, #{amount}, #{type}, #{memo})
    </insert>

</mapper>